{
  "rewrites": [
    { "source": "/api/:slug/:id", "destination": "/api/dispatch?slug=:slug&id=:id" },
    { "source": "/api/:slug", "destination": "/api/dispatch?slug=:slug" },
    { "source": "/((?!api/).*)", "destination": "/" }
  ]
}
